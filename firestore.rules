rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The browser never accesses POS records directly. All reads and writes go
    // through authenticated callable functions, which use the Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
